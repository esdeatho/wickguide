/* const Discord = require('discord.js');

module.exports = {
    name: 'echo', // The name of the command
    description: 'what this bots about', // The description of the command (for help text)
    aliases: ['e'],
    args: false, // Specified that this command doesn't need any data other than the command
    usage: '', // Help text to explain how to use the command (if it had any arguments)
   // aliases: 's',
   
   execute(args,message) {

    if (!args[0]) return message.channel.send("error");

    if (args[0].split("/")) {
        messages.fetch(message).then(message => {
            message.content 
            })

            channels.cache.get(args[0].split("/")[5]).messages.fetch(args[0].split("/")[6]).then(message => {
                message.content //oh wow message content!!! That was so difficult to get :(((( jk ily <3
                })
                
         console.log(args[0].split("/"))
    message.channel.send('meow');
   }}}
   */ 